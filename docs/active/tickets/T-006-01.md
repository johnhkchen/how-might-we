---
id: T-006-01
story: S-006
title: fix-streaming-partial-dedup
type: bug
status: open
priority: critical
phase: done
depends_on: []
---

## Context

The expand and refine stages generate infinite variant cards. Root cause: BAML streams progressively-building partial objects where each SSE event contains the full array so far with partially-complete entries. The frontend treats each partial `statement` string as unique (since it grows token by token), adding a new candidate for every streaming update instead of waiting for complete variants.

The field `moveType` on `HMWVariant` is the completeness signal — it appears only when a variant object is fully formed. The fix must wait for `moveType` before adding a candidate, and track by array index to avoid reprocessing.

This same issue affects both `expandHMW()` and `refineHMW()` in the workshop page.

## Acceptance Criteria

- [ ] Expand stage produces exactly the number of variants BAML returns (typically 6-8), not hundreds
- [ ] Refine stage produces exactly the number of new variants BAML returns, not hundreds
- [ ] Variant cards appear one at a time as each variant completes during streaming
- [ ] No duplicate candidates in the session store after streaming completes
- [ ] Works with both real API and mock fixtures
- [ ] Existing Playwright tests still pass
